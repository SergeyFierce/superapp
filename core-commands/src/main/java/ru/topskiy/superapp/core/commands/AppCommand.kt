package ru.topskiy.superapp.core.commands

/**
 * Маркерный интерфейс для всех системных команд приложения.
 *
 * Отличие от [AppEvent]:
 *   Event  — «что-то произошло» (прошедшее время, реакция необязательна)
 *   Command — «сделай это» (намерение, ожидается конкретный обработчик)
 *
 * Сервисы отправляют команды через [CommandBus] и подписываются
 * на нужные типы без прямых зависимостей друг от друга.
 */
interface AppCommand
